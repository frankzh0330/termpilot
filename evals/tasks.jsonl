{"id": "fix-python-test", "prompt": "Fix the failing test in this project. Run pytest to verify.", "workspace": "templates/fix-python-test", "verifier": "python -m pytest -q", "max_turns": 10, "timeout": 120}
{"id": "create-cli", "prompt": "Create a CLI script called hello.py that takes a name as sys.argv[1] and prints 'Hello, {name}!'. Include a basic test.", "workspace": "templates/create-cli", "verifier": "python -c \"import subprocess; r=subprocess.run(['python','hello.py','World'],capture_output=True,text=True); assert r.stdout.strip()=='Hello, World!', r.stdout; print('PASS')\"", "max_turns": 10, "timeout": 120}
{"id": "refactor-function", "prompt": "Refactor the add() function in main.py to also support subtraction via a mode parameter (mode='sub' or mode='subtract'). Keep all existing tests passing.", "workspace": "templates/refactor-function", "verifier": "python -m pytest -q", "max_turns": 12, "timeout": 150}
{"id": "run-command", "prompt": "Read data.txt and count how many lines contain the word 'error'. Write just the number to result.txt.", "workspace": "templates/run-command", "verifier": "python -c \"c=open('result.txt').read().strip(); assert c=='3', f'Expected 3 got {c}'; print('PASS')\"", "max_turns": 8, "timeout": 90}
{"id": "multi-file", "prompt": "Fix the import error and the bug in utils.py so that all tests pass. Run pytest to verify.", "workspace": "templates/multi-file", "verifier": "python -m pytest -q", "max_turns": 12, "timeout": 150}
